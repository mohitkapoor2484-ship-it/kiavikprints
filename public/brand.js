(function(){const $=(s,c=document)=>c.querySelector(s),$$=(s,c=document)=>[...c.querySelectorAll(s)];document.addEventListener('DOMContentLoaded',async()=>{const mb=$('.menu-button'),nav=$('.main-nav');if(mb&&nav)mb.onclick=()=>nav.classList.toggle('open');let count=JSON.parse(localStorage.getItem('kiavik_cart')||'[]').reduce((a,b)=>a+(b.quantity||1),0);let hc=$('#headerCartCount');if(hc)hc.textContent=count;let home=$('#homeProducts');if(home){try{let r=await fetch('/api/bootstrap'),j=await r.json(),ps=(j.products||[]).slice(0,6);home.innerHTML=ps.map(p=>`<article class="product-card"><div class="product-card-media">${p.imageData?`<img src="${p.imageData}" alt="${esc(p.name)}">`:`<div class="placeholder-art">${esc(p.name)}</div>`}</div><div class="product-card-body"><span class="product-badge">${esc(p.category||'Kiavik')}</span><h3>${esc(p.name)}</h3><p>${esc((p.description||'Custom 3D printed product').slice(0,92))}</p><div class="product-card-footer"><strong>$${esc(p.priceLabel||'0.00')}</strong><a class="mini product-link" href="/shop/?product=${encodeURIComponent(p.id)}">View options →</a></div></div></article>`).join('')||'<p>Products are being added now.</p>'}catch(e){home.innerHTML='<p>Visit the shop to see the current catalogue.</p>'}}});function esc(v){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}})();